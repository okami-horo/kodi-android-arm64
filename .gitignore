# General
.DS_Store
Thumbs.db
*.log
*.tmp
*.swp
/.vscode/
/.idea/
*.iml

# Gradle/Android build
/.gradle/
/build/
**/build/
/local.properties
/.cxx/
/.externalNativeBuild/

# Android artifacts
*.apk
*.aab
*.ap_
*.dex
*.idsig

# Signing/credentials (never commit secrets)
*.keystore
*.jks
*.p12
*.pem
keystore.properties

# NDK / Makefile outputs (keep source assets & libs tracked)
# Do NOT ignore xbmc/assets or xbmc/lib so they can be versioned
/xbmc/obj/
/xbmc/java/
# Android app resources (track in VCS)
# 说明：为保证 Gradle 直接构建可复现，这里不再忽略 mipmap 与颜色资源
# 如需重新生成，可通过 Makefile 的 res 目标从 media/ 拷贝覆盖
# （依然保留对构建产物与大体积调试符号文件的忽略）


# IDE noise
/.settings/
/.project
/.classpath

# Python caches (do not ignore real .py scripts globally)
__pycache__/
*.pyc

# Local Gradle user home used by this repo
/.gradle-user/

# Android Studio captures and mappings
captures/
**/mapping/

# Patch/merge leftovers
*.orig
*.rej

# Unstripped backup (do not commit)
xbmc/lib/**/libkodi.unstripped.so
